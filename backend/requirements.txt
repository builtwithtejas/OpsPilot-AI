fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy[asyncio]==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
PyGithub==2.4.0
python-multipart==0.0.12
slowapi==0.1.9
httpx==0.27.2
tenacity==9.0.0
psutil==5.9.8
pytest==8.3.3
pytest-asyncio==0.23.8

# FIX: pytest-anyio does not exist on PyPI. test_health.py uses @pytest.mark.anyio
# which requires the anyio pytest plugin, shipped as part of anyio itself.
# Install anyio[trio] and use pytest-anyio via the anyio package — no separate package needed.
# The correct pytest plugin is registered automatically when anyio[trio] is installed.
anyio[trio]>=4.0

# Async DB drivers
asyncpg==0.29.0
aiosqlite==0.20.0

# Google Cloud / Gemini
google-generativeai==0.8.3
google-cloud-aiplatform==1.71.1

alembic==1.13.3
python-jose[cryptography]==3.3.0
google-adk